@model Data.Order
<div>
    <h1>Submit Your Order</h1>
    <div>
        <!--@Html.ValidationSummary()-->
        @using (Html.BeginForm())
        {
            <div>
                <label>Your Name</label>
                @Html.EditorFor(m => m.OrderOwner)
                @Html.ValidationMessageFor(m => m.OrderOwner)
            </div>
            <div>
                <input type="submit" value="Submit" />
            </div>}
    </div>
</div>